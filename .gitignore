# build output
dist/
# astro cache + generated types
.astro/
# deps
node_modules/
# env / logs / os
.env
.env.production
*.log
.DS_Store
# author delivery archives (zip drops — never commit)
archives/
reference/
# blog drafts (placeholder versions — never commit; published copy lives in src/content/blog/)
drafts/
