# build output
dist/
# astro cache + generated types
.astro/
# deps
node_modules/
# env / logs / os
.env
.env.production
*.log
.DS_Store
# author delivery archives (zip drops — never commit)
archives/
